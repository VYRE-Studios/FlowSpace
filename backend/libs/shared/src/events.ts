export enum WorkspaceEvents {
  MESSAGE_SENT = 'workspace.message.sent',
  FILE_UPLOADED = 'vault.file.uploaded',
  MEETING_STARTED = 'workspace.meeting.started',
  PRESENCE_UPDATE = 'workspace.presence.update',
  CHANNEL_CREATED = 'workspace.channel.created',
}
